#include <stdio.h>

#define PI 3.14159

int main() { float radius, height, volume, surface area;

// Prompt user for input. printf("Enter the radius of the cylinder: "); scanf("%f", &radius); printf("Enter the height of the cylinder: "); scanf("%f", &height);

//Calculate volume volume= * radius radius height

// Calculate surface area surface area = 2 PI radius radius 2 PI radius height;

// Display results printf("Volume of the cylinder: %.2f\n", volume); printf("Surface area of the cylinder: %.2f\n", surface_area);

return 0;

}