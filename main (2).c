#include <stdio.h>

int main() { float weight, height, bmi;

// Prompt user for input. printf("Enter your weight in kilograms: "); scanf("%f", &weight); printf("Enter your height in meters: "); scanf("%f", &height);

// Calculate BMI bmi = weight/ (height height);

// Display the BMI printf("Your BMI is: %.2f\n", omi);

return 0;

}